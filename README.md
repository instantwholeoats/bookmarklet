# Trello bookmarklets

- `trello_number.js` numbers cards in each visible Trello list.
- `trello_export.js` numbers the cards and downloads their text as `trello-cards.txt`.

The scripts have no third-party dependencies. They build labels with DOM APIs rather than inserting HTML strings.

Run the checks with `node --test`.
